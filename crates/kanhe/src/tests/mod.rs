//! The failure matrices: unit tests beside the judgements they test.
//!
//! Split by topic the way 圭表 and 渾儀 already split theirs, so a matrix sits next to what it tests
//! rather than in a separate binary reaching back in through `#[path]`.

mod bound_register_parse;
mod capability_subjects;
mod gate_identity;
mod hermetic_git;
mod manifest;
mod publish_source_gate;
mod reading;
mod record;
mod release_coherence_gate;
mod repository_path;
mod restatement;
mod sections;
mod selection;
mod wrapper_parser;
